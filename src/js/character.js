/**
 * Функция конструктор Character;
 * @param {string} name имя пользователя.
 * @param {string} type тип оружия.
 * @throws {InvalidArgumentException}
 */

// eslint-disable-next-line no-unused-vars
function Character(name, type) {
  this.name = name;
  this.type = type;
}
